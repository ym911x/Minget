# 1.2.1 审核状态

## 当前结论

代码修订、310 项自动化测试和 1.2.1 Release 构建已完成，严格签名验证通过。用户提供的真实运行截图确认两项修订均已生效。

## 审核边界

- 金额格式化只改变展示结果，不回写 `Decimal`。
- Command Code 卡片已移除 `ProviderUpdatedFooter`，详情页顶部刷新状态保持不变。
- DeepSeek 等其他卡片的更新时间行为不在本次范围内。
