# 1.2.1 实施报告

## 修改范围

- 新增固定两位小数的美元显示格式，并用于 Command Code 的窗口金额与统计成本。
- 删除 Command Code 卡片底部的单独更新时间。
- Command Code 可见时，详情窗口高度减少 20 点。

## 数据边界

本次没有修改接口请求、认证、解析、缓存或原始金额。金额舍入仅影响界面文字。

## 验证

2026-09-15 完整执行 `swift test`，310 项测试通过、0 项失败。`./scripts/build.sh` 完成 1.2.1 Release 构建，staging、安装目录和 `dist/Minget.app` 的签名验证均通过。用户随后提供真实运行截图，确认金额固定两位小数、卡片底部没有单独更新时间，且内容完整。
