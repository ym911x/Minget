# 1.3.2 验收台账

当前状态：每日多时点点火和 Command Code 点火已实现，全量测试、脱敏渲染目检、隔离 Release、签名和安全扫描均通过，Command Code 真实手动点火已由用户确认成功。用户于 2026-09-20 接受并授权发布 1.3.2；发布提交 `c51791b`、annotated tag `v1.3.2` 和 [GitHub Release](https://github.com/ym911x/Minget/releases/tag/v1.3.2) 已完成。真实睡眠/唤醒、完整界面与 Command Code 定时点火保留为发布后验证项，不改记为通过。

| 项目 | 当前状态 | 验收依据 |
| --- | --- | --- |
| 点火 live 前值门槛 | 通过 | live/stale/unavailable/nil、首次提前结束与两次无证据均有回归 |
| 轻量确认不读身份 | 通过 | fake client 断言确认路径零 `account/read`，只读额度 |
| 差值与最近 3 条历史 | 自动与渲染通过 | 59/60 边界、三档格式、截断顺序、三态浅色/深色均覆盖 |
| Command Code 缓存隔离 | 通过 | 完整摘要跨凭证隔离、组件分别过期、部分失败、回退、旧缓存解码 |
| force 与凭证代次竞争 | 通过 | 自动读取后补 force，全量读取；旧代结果不提交 |
| 组件缓存 UI | 自动与渲染通过 | 套餐、统计、月度行按组件标缓存；5 小时/周额度保持实时 |
| 三目标每日多时点 | 自动通过 | 添加、勾选、修改、删除、持久化、10 分钟补跑、跨重启去重与跨午夜警告均有回归 |
| Command Code 手动点火 | **真实通过** | 用户在 `/tmp/minget-132-pathfix-run/Minget.app` 确认手动点火成功；真实 CLI、Keychain Key、最小请求和 credits 确认链路贯通 |
| Command Code 定时点火 | 自动通过，真实请求待验收 | fake CLI 固定参数、隔离 HOME、Key 最小传递、输出丢弃、超时/stop 回收、后台 Keychain 策略 |
| Command Code 点火确认 | 自动通过 | CLI 成功后仅读 credits，复用 live 前值、三态、差值和历史 |
| 30/60/120 节奏与 tick | 通过 | 基础间隔和时钟间隔均验证默认值与注入值 |
| 唤醒探活 | 自动通过，真实睡眠待验收 | 健康只读额度，失败 Profile 才完整刷新，熔断/stop 抑制 |
| 30 秒互斥 | 自动通过，真实睡眠待验收 | 连续唤醒与定时轮询节流有确定性测试 |
| R2 AX XCTest | 修复完成 | 每轮只遍历新窗口；无 AX 窗口时明确跳过 |
| 独立 AX 实测 | 通过 | 两个独立 AXLink、身份 AXStaticText、两次 AXPress 各触发 1 次 |
| 固定布局 | 自动与脱敏渲染目检通过 | ChatGPT 416 × 129、Command Code 416 × 176、四卡页 440 × 566、设置页 520 × 700；浅色/深色、多时点、警告和 footer 无截断 |
| 全量自动测试 | 通过，含 1 项明确跳过 | Core 360 + App 146 = 506 项；505 通过、1 跳过、0 失败 |
| Release 与签名 | 通过 | 版本 1.3.2；arm64；staging/install/archive 严格签名通过 |
| 安全与清理 | 通过 | `git diff --check`、冻结目录、端点/敏感信息和进程归属扫描通过；每次 CLI 使用唯一临时 `HOME` 并在结束后删除，无残留目录 |
| 真实签名包完整界面 | **发布后待验证** | 当前 UI 控制接口无法绑定菜单栏状态项，未伪写通过；用户接受不阻断本次发布 |
| 真实睡眠唤醒 | **发布后待验证** | 需要用户触发睡眠/唤醒并现场复核；用户接受不阻断本次发布 |
| 真实 DeepSeek 菜单栏 | 沿用待验收 | 与详情真实余额核对 |
| A/B 真实应用内点火 | 需另行授权 | 会调用模型并消耗少量额度，本轮未执行 |
| Command Code 真实手动点火 | **通过** | 2026-09-20 用户确认 PATH 修复签名包手动点火成功 |
| Command Code 真实定时点火 | **发布后待验证** | 临时添加一条短时定时并勾选，确认仅触发一次且无后台 Keychain 弹窗；用户明确要求后续再验证 |
| 机器完整重启缓存隔离 | 沿用待验收 | 重启后确认 A/B 不串号 |

## 发布后跟进

1. 打开隔离签名包，检查 440 × 566 详情页、520 × 700 设置页、三组多时点计划、Command Code 按钮和 footer 无截断；悬停结果确认最多 3 行。
2. 触发一次真实睡眠/唤醒，在 30 秒内确认 A/B 数据恢复；没有重复刷新风暴、账号串用或孤儿 `codex app-server`。
3. 临时添加一条几分钟后的 Command Code 计划并勾选；确认只发起一次请求，定时路径不弹 Keychain 授权窗。手动点火已通过。

用户于 2026-09-20 明确接受先闭合并发布 1.3.2，以上三项移至发布后验证，不因发布改记为通过。真实 A/B 点火不属于本轮自动执行范围，必须另行明确授权。

凭证、真实邮箱、余额、API Key、OAuth token、原始响应、Codex CLI session id 和 raw output 禁止写入本目录或验收截图。
