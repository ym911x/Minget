# 1.4.2 验收台账

状态：实现、独立审核、538 项 Swift 测试（1 项环境跳过、0 失败）、53 项外部脚本断言、严格签名安装及自然定时运行核验完成。真实界面三次手动刷新已观察到数据返回；用户于 2026-09-24 确认详情页关闭重开三次均正常。第三轮聚合结束标志未单独留证，保留此证据边界。 GitHub 发布核验见 ACCEPTANCE.md；不承诺服务端窗口必然推进。

## 1. 已完成（自动化，2026-09-23）

| 项目 | 命令 | 结果 |
| --- | --- | --- |
| 全套 Swift 测试 | `swift test --disable-sandbox --scratch-path /tmp/minget-142-independent-tests` | Core 380 项 0 失败；App 158 项 1 项 AX 环境跳过、0 失败；合计 538 项执行，537 通过、1 项跳过 |
| 单飞等待者语义 | 同上（`UsageServiceTests`） | 38 项通过：合并、加入实际结果、恰好一次补刷、停止释放等待者、补刷在确认读取后仍运行 |
| 唤醒探测占槽 | 同上 | 探测持槽、加入者得实际结果、失败升级为真实轮次 |
| 退避梯子 | 同上 | 立即重试一次；30/60/120/300 封顶；成功清零；手动旁路；不可重试错误不循环 |
| 超时拆分 | 同上 | quota 15 s / handshake 5 s / identity 3 s 逐方法记录断言 |
| 身份状态机 | 同上（`CodexProfileIdentityTests`） | 9 项通过：同代次身份缺失降级、重连/重试换代清除、退出登录失效、替换及只读确认不错误归因 |
| 自动重试调度 | 同上（Coordinator + App cadence） | retry deadline 纳入 timer；最早 profile 到期时间、常规周期、需手动处理错误均有测试 |
| 外部脚本模板测试 | `scripts/minget-fire/tests/run_tests.sh` | 53 项断言通过；沙盒假 CLI，无网络、无模型请求 |
| 读取器协议 | 同上 | 分包/粘包 JSON、单一总期限（2 s 截断静默服务）；直接 child 先退出、grandchild 忽略 TERM 时仍整组 SIGKILL 回收 |
| 超时执行器 | 同上 | 超时码 124；正常主进程退出后清理顽固后代，超时后回收主/子进程 |
| 退出码语义 | 同上 | 观测未变化/不可用 exit 0；超时 124 且后续目标继续；聚合退出码正确 |
| 干净日志 | 同上 | 全部行匹配固定词表正则；无身份标记泄漏 |
| 安装器演练 | 隔离 `HOME` 假 CLI 套件 + 真实只读 `scripts/install-minget-fire.sh --check` | 不认识的现有文件在任何备份/目录/替换前被拒绝；当前模板幂等识别；全新安装 hash/权限正确并输出逐文件回滚命令；真实三个目标与记录的旧版 hash 匹配，检查无写入 |

说明：基线对照见 INDEPENDENT_REVIEW.md（1.4.1 基线 517 项自动测试，1 项环境跳过）。最终 Core 380 项、App 158 项，合计 538 项执行、1 项既有 AX 环境跳过、0 失败。

## 2. 已完成与未完成（发布门槛）

- [x] Release arm64 构建与严格签名检查；安装前保留 1.4.1 签名包备份。1.4.2 安装包版本为 1.4.2，架构 arm64，严格签名有效。
- [x] 签名包真实界面验收：三次手动刷新均被接受，前两轮观察到刷新结束，第三轮已观察到 A/B 数据及身份、Command Code 返回；随后浮窗不可访问，未单独留证第三轮聚合完成标志。用户于 2026-09-24 明确确认“关闭再打开详情页，重复 3 次，都能正常显示”，完成最终界面验收。工具证据与用户实测分开记录。
- [x] 真实恢复路径：B 从缓存及上次成功时间恢复到实时额度；身份暂不可用时仍显示额度，后续恢复套餐和邮箱；A 独立更新，不被 B 缓存状态阻塞。顶部失败时显示“部分数据未更新”。本机签名在完整访问环境下再次严格验证通过。
- [x] 新模板脚本经 `scripts/install-minget-fire.sh` 显式安装，三个旧文件均有同 hash 备份；安装器未修改 plist 或重载 launchd。
- [x] 安装后首个自然定时运行已核验，LaunchAgent 保持加载，plist SHA-256 与安装前一致：`0a54ea2d961e56837a211b3c0b8f190ef6c0bbaacf7357efd045ece2b48518ea`，时间仍为 05:30 / 10:35 / 15:40。
- [ ] GitHub Release 与发布提交 CI。

### 2026-09-23 自然定时运行

- launchd 在 15:40 触发，脚本 15:40:13 开始；A、B、Command Code 严格顺序执行，进程间保留至少 15 秒间隔；launchd `runs = 10`，`last exit code = 0`。
- A：CLI 请求进程 `exit=0`，耗时 29 秒；5 小时窗口前值缺失，无法确认重置时间变化。只能确认请求进程成功，不能确认窗口已推进。
- B：CLI 请求进程 `exit=0`，耗时 51 秒；前后服务端重置时间相同，差值 0 秒。窗口未变化属于观测结果，不构成请求失败。具体额度与重置时间仅保留于本机日志。
- Command Code：CLI 请求进程 `exit=0`，耗时 5 秒。
- 原始日志仅包含 1.4.2 模板固定字段，证据在 `~/Library/Logs/Minget/fire-A.log`、`fire-B.log`、`fire-commandcode.log`；未复制模型输出、身份原文或认证内容。
## 3. 验收边界

- 没有手动触发点火。上述请求来自用户原有 LaunchAgent 的自然运行。
- 未提交、未推送、未发布。
- 未读取全局 Codex/Claude 配置、认证文件或 Profile 内容；真实外部脚本仅通过已授权的 CLI 点火路径运行，日志为固定脱敏字段。
- 自动脚本测试使用 `/tmp` 沙盒假 CLI；真实自然运行是单独的验收证据，不以测试替代。
- macOS 于真实界面第一轮刷新后锁屏，CUA 明确报告需用户解锁；未尝试绕过锁屏，也未将未完成的 UI 轮次记为通过。
- 晚间解锁后的补测如上；截图和辅助功能树含真实身份及额度，仅作本机验收，不加入公开仓库。回滚步骤见 [ROLLBACK.md](ROLLBACK.md)。
