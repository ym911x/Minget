# 1.2.1 需求

## 目标

修正 Command Code 详情卡的数字精度和重复刷新信息。

## 展示要求

- Command Code 的已用、剩余和成本金额固定显示两位小数。
- 舍入只发生在显示层，不修改接口解析、缓存或底层 `Decimal` 值。
- 移除 Command Code 卡片底部的“刚刚更新”“更新于 X 分钟前”等文字。
- 保留详情页顶部的全局刷新状态，并收紧删除底部文字后的卡片占用空间。

## 非目标

不修改 Command Code 接口、认证、缓存、统计周期口径、菜单栏内容或其他服务卡片。
