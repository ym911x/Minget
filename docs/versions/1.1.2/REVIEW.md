# 1.1.2 审核状态

状态：已完成（代码、自动测试、构建和真实详情界面已核查）

## 审核结论

- 菜单栏空间状态只包含 `.full` 和 `.compact`。完整模式压缩到紧凑模式后仍保留 `5H`、`W` 和两排时间条；紧凑模式没有更小的生产降级路径。
- `availableCount` 是 OpenAI 重置数量的唯一权威来源。到期时间只从有效、未过期的明细中取最近值。缓存、缺失、负数、非法类型和过期明细不会被显示为当前权益。
- `UsageSnapshot.Persisted` 只增加可选的数量和时间字段，旧缓存可以解码，未保存重置 ID、上游标题、描述或凭证。
- DeepSeek 保持既有余额请求和允许路径。官方余额接口没有账号名称或邮箱字段，详情页不生成账号占位信息，也不读取浏览器会话；真实余额直接放在品牌同列并垂直居中，状态位于 Logo 下方。
- 详情页移除纵向滚动容器，固定高度覆盖 DeepSeek 多币种和未连接状态；自动测试覆盖多币种、未连接、缓存、状态页不可用和无滚动容器。

## 证据

- `swift test`：301 项通过，0 失败。
- `./scripts/build.sh`：Release 构建、安装和归档校验通过。
- `MingetCLI --timeout 10`：真实 `codex app-server` 握手成功，当前样本返回两个可用重置并带最近到期时间，缓存往返通过。
- CUA 真实详情窗口：确认 1.1.2 版本、OpenAI 重置行、DeepSeek 同列右侧余额和 Logo 下状态，未见滚动条。
- `codesign --verify --deep --strict` 和 Info.plist 版本检查通过。

本轮没有保存公开的菜单栏全局截图；CUA 实际核查了详情窗口，菜单栏状态机、宽度重测、睡眠唤醒、屏幕变化和菜单增减由自动测试与 AppKit 接线覆盖，安装包也已用正常 `open` 启动检查。
