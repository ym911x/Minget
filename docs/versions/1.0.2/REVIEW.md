# 明明有数 · Minget v1.0.2 审核状态

## 当前状态：审核与用户验收通过

本文件由实施方填写，只记录当前版本状态。实施方自测与自动测试不能替代独立审核，也不能替代用户验收。

| 阶段 | 状态 | 依据 |
| --- | --- | --- |
| 需求与边界整理 | 已完成 | `REVISION_SPEC.md` |
| 应用实现 | 已完成 | `IMPLEMENTATION_REPORT.md` 第 2 节 |
| 自动化测试 | 已执行，359 项通过、0 项失败 | `evidence/logs/01-test-and-repo.txt`、`12-keychain-revision-build.txt` |
| 本地候选构建 | 已执行，staging 与运行路径严格校验通过 | `evidence/logs/02-build-and-bundle.txt`、`12-keychain-revision-build.txt` |
| 真实进程运行 | 已执行，含 1 项缺陷的发现与修复 | `evidence/logs/03`、`04`、`05` |
| 真实菜单栏点击 | **用户验收通过** | 2026-09-12 用户确认点击桌面或其他应用后详情立即收起，原点击有效 |
| Codex 独立审核 | **已通过** | 2026-09-11 复核源码差异、359 项测试、Release 构建、签名、版本与脱敏日志 |
| 用户最终体验验收 | **已通过** | 钥匙串弹框与菜单栏外部点击收起均由用户实测确认 |
| 发布与推送 | 已获授权，执行中 | 用户确认最后一项真实点击验收通过后要求上传 GitHub |

## 独立审核结论

1. 四项菜单栏修订均已落入 `REVISION_SPEC.md` 定义范围。品牌图标已移除；时间条使用真实 `resetsAt`；尾部警告已删除；弹层关闭监听只在弹层显示期间存在并返回原事件。
2. `ResetTimeProgress` 对五小时、周窗口、部分段、到期、未知、非法与 60 秒容差的处理符合异常矩阵。时间更新不增加服务请求，也不按本地时钟虚构新周期。
3. 首次测宽修复属于满足空间模式要求的必要调整。防抖状态机、状态项位置保存键和最小模式持久化值均保留。
4. 钥匙串改造未改变余额字段、服务端点与凭证保存边界。所有生产凭证访问集中到协调器；状态查询不重复读取；同一凭证并发读取合并；日志不包含凭证值。
5. 2026-09-11 独立执行 `swift test`，结果为 359 项通过、0 项失败；重新执行 `./scripts/build.sh` 成功。`~/Applications/Minget.app` 的版本为 1.0.2、Bundle Identifier 未变、严格签名校验通过。
6. 发布前敏感信息扫描未发现真实 API Key、Cookie、Authorization 值、私钥或证书文件。命中的 `sk-` 字符串仅位于合成测试夹具。

## 未解决或需要独立复核的事项

- 真实菜单栏外部点击已由用户在实际界面确认通过。离屏渲染、纯判定和监听生命周期测试也已通过。
- `?` 状态徽标为整块居中一个，与本轮方案 §4.3 的「按排绘制」有偏离，理由见 `IMPLEMENTATION_REPORT.md` 第 7 节第 2 条。
- 历史候选包曾出现启动延迟；稳定签名与协调器实施后，用户实测只出现一次授权弹框，选择“始终允许”后不再重复出现。
- `dist/` 下存在 1.0.0 时期的两个旧包 `UsageMonitor.app` 与 `UsageMonitor 2.app`，DR 与 `Minget.app` 各不相同，会互相覆盖钥匙串授权记录。用户已确认暂不清理，仅记录。

## `SIGNING_HANDOFF.md` 与 `KEYCHAIN_REVISION_PLAN.md` 的当前状态

- 自签名证书 `Minget Local Signing` 已创建（SHA-1 `5A48BB2B…`），`codesign` 无提示即可使用私钥，说明私钥授权已就绪。
- 候选包运行路径为 `~/Applications/Minget.app`（iCloud 之外，严格校验通过）；`dist/Minget.app` 为归档副本，普通校验通过、严格校验受 iCloud file provider 属性影响，差异由构建脚本显式记录。
- 钥匙串重复弹框：P0 与 P1 已实施，自动化测试覆盖。用户已实测确认首次授权后不再重复弹出，真实取数成功。后台轮询、换 Key、锁屏唤醒等扩展场景继续列在验收台账中观察。

## 历史审核

v1.0.0 的审核与验收结论保留在 `docs/archive/v1.0/`，本轮不覆盖、不改写。
