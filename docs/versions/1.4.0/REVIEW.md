# 1.4.0 审核状态

状态：实现审核和发布验收通过，待创建标签与 GitHub Release。当前没有把自动测试结果冒充真实低额度服务轮询。

## 审核结论

- 应用内 Command Code 点火模型已集中由 `CommandCodeFireService.modelName` 提供，固定参数回归会锁定 `deepseek/deepseek-v4.1-flash`。
- 外部 `minget-fire` 已核对为同一模型标识；本版本没有扩大到 LaunchAgent 时间表或权限修改。
- 低额度策略只在当前菜单栏选择上计算，补充 timer 不替代也不重写既有详情/提供方刷新。
- ChatGPT 使用 5 小时 OR 周额度的严格 `<` 比较；DeepSeek 只在当前显示币种为 CNY 时比较金额；未知值不触发。
- 任务、timer 和偏好对象没有读取凭证、缓存文件或全局认证配置的路径。

## 清单

- [x] 模型常量、应用内参数和测试已统一为 `deepseek/deepseek-v4.1-flash`。
- [x] 低额度偏好版本化持久化，不包含 Key、token、Cookie 或缓存数据。
- [x] 15/30/60 秒周期、默认值、阈值归一化和恢复默认已实现。
- [x] ChatGPT 5 小时/周边界和 OR 关系有回归测试。
- [x] DeepSeek CNY、非 CNY、恰好阈值和无效金额有回归测试。
- [x] 当前菜单栏来源隔离、禁用策略、正常周期上限和 timer 停止路径有回归测试。
- [x] 全量测试最终通过：Core 360、App 153；512 项通过、1 项既有 AX XCTest 明确跳过、0 失败。
- [x] timer 生命周期回归：安装、来源切换取消、重新启用、关闭开关和 stop 清理均通过。
- [x] Release arm64 构建与严格签名：版本 1.4.0，staging/install/archive 均通过。
- [x] 已安装签名包完成真实退出与重新启动；运行二进制固定模型字符串核对通过。
- [x] 设置页明暗确定性渲染检查通过，新控件无截断或重叠。
- [ ] 真实低额度状态下的服务轮询现场观察；不执行额外真实模型请求，不阻断本次源码发布。

## 审核边界

本版本只改变应用内点火模型标识和菜单栏补充刷新。LaunchAgent 当前文件属于外部运行态，本轮只做只读核对，不改时间表；其真实触发仍需用户另行验收。自动测试使用 fake service/provider，不消耗额度。
