# 1.3.0 验收台账

当前状态：第三轮界面微调已经实现并完成自动验证。用户于 2026-09-17 提供最终真实运行截图并确认详情页修订完成；公开截图已将邮箱、余额、额度、时间和统计数据替换为示例值。真实 DeepSeek 菜单栏余额与 A/B 两次真实点火仍待后续执行。任何项目只有取得对应真实证据后才能改为「通过」。

| 项目 | 状态 | 验收依据 |
| --- | --- | --- |
| Profile 模型与默认迁移 | 通过（自动 + 真实界面） | 两个稳定 Profile，路径为当前用户主目录下的相对配置，不含个人绝对路径；用户最终截图确认两张 Profile 卡片稳定显示 |
| 双 app-server 隔离 | 通过（真实包实测） | 两个 fake 子进程隔离测试；签名包启动后两个真正兄弟 `codex app-server` 子进程分别实测到 `~/.codex-minget-a` 与 `~/.codex-minget-b`；两个真实 `CODEX_HOME` 目录内容未读取 |
| A/B 额度与账号归属 | 通过（真实界面） | 用户最终截图确认两个 Profile 分别显示不同真实账号、套餐和额度；文档与公开素材不记录真实邮箱和数值 |
| Profile 级缓存隔离 | 自动通过，机器重启待验收 | `UsageCacheTests` 覆盖两 Profile 互不可见、按 Profile 的 last-known account、并发与失败；三次真实启动/退出无串号，机器重启后待复验 |
| 旧缓存迁移 | 自动通过 | 同 ID 迁移、不同 ID 删除、缺失 ID 删除、B 永不接收、只执行一次且不覆盖新数据 |
| 菜单栏选择持久化 | 自动通过，真实切换待验收 | `MenuBarPreferencesTests` 覆盖三项切换、重启保持、损坏回退账号 A |
| ChatGPT 菜单栏内容 | 自动通过，真实几何待验收 | A/B 短标签、双额度、双时间条、缓存警告、完整/紧凑固定格式；渲染证据见 `$TMPDIR/Minget-1.3.0-Evidence/` |
| DeepSeek 菜单栏余额 | 自动通过，真实余额待验收 | 币种优先级、金额语义、无余额不显示 0；真实余额与详情一致待做 |
| 刘海和空间降级 | 自动通过，真实几何待验收 | 三来源完整/紧凑宽度实测；真实几何检查待做 |
| 冷启动无空窗口 | 通过（真实包实测） | 生产入口不再声明任何 SwiftUI Scene；`StartupWindowTests` 启动签名包后断言该进程拥有的 layer-0 窗口数为 0，本机实测通过 |
| 详情页双账号 | 通过（自动 + 真实界面） | 单列 A→B、状态互不污染；用户最终截图确认两张卡片各自显示独立账号、套餐、额度与分段时间轨道 |
| 详情页无滚动布局 | 通过（自动） | 视图树无 `ScrollView`/`NSTableView`/`NSOutlineView`；`440 × 552 / 498 / 384 / 330` 四种页面状态均可渲染；卡片在自己的固定 frame 内布局，无裁切 |
| 详情页屏幕边界 | 通过（自动 + 当前屏幕实测） | 用户最终截图确认 440 pt 弹层完整显示；自动测试继续覆盖 `show` 前 contentSize、按钮中点锚定、屏幕容纳降级和普通详情窗口 clamp |
| Command Code 额度方向与时间线 | 通过（自动 + 真实界面） | fraction 使用 `remaining / limit`；用户最终截图确认 `$余额 / $总计`、绝对重置时间、月度剩余天数及 5/7/连续时间轨道均完整显示；公开记录不保留真实数值 |
| DeepSeek 菜单栏与详情卡 | 详情卡真实通过，菜单栏待验收 | 用户最终截图确认 `416 × 48 pt` 单行详情卡；菜单栏 `DS CNY 123.45` 的 13/12 pt 两种模式已自动验证，真实菜单栏仍待核对 |
| 账号 A 点火按钮 | 待用户验收 | 自动测试使用 fake executable；需真实执行一次并按固定文案报告 |
| 账号 B 点火按钮 | 待用户验收 | 同上 |
| 点火结果语义 | 通过（自动），真实窗口待验收 | 只有 `isLive == true` 且前后两个 `resetsAt` 相差 ≥60 秒才算确认；缓存成功触发 5 秒后重试，两次都缓存时结果为「请求成功，窗口未变化」 |
| 点火并发与重复点击 | 自动通过 | 同 Profile 第二次立即 `.alreadyRunning`；A/B 可并行；超时只对本次 PID 终止 |
| 凭证与日志边界 | 自动通过 | 源码无认证 JSON 读取；点火服务 drain 后丢弃 stdout/stderr；诊断只记录固定分类 |
| 既有服务回归 | 自动通过 | DeepSeek、Command Code、Keychain、显示开关与刷新逻辑测试全部保持通过 |
| 现有点火基础设施保持 | 自动通过 | `minget-fire` 与 LaunchAgent 修改时间未变化（20:03 / 20:07），本轮未调用 |
| 自动化测试 | 自动通过 | `swift test --scratch-path /tmp/minget-revision-scratch`：Core 325 + App 100 = 425 项通过，0 失败 |
| Release 构建与签名 | 自动通过 | `./scripts/build.sh`；staging 与 `~/Applications/Minget.app` 严格签名通过，`CFBundleShortVersionString = 1.3.0` |
| 子进程清理 | 通过（自动 + 真实包实测） | 三次退出确认 app-server 无遗留；`stopAll()` 终止并回收运行中的 A/B 点火子进程且不误杀无关 PID；`stop()` 路径的终止由 `FireLifecycleTests` 覆盖；签名包退出后全机无进程引用 `~/.codex-minget-*` |
| 详情页真实界面确认 | 通过 | 用户于 2026-09-17 提供最终运行截图并确认“好了”；440 pt 单列、两张 ChatGPT 卡、DeepSeek 单行卡与 Command Code 三周期卡均完整，无滚动和裁切 |
| 菜单栏真实切换与两个点火按钮 | 待验收 | DeepSeek 菜单栏真实余额和 A/B 两次真实点火尚未执行，不纳入本次发布完成结论 |
| 发布、标签、远端推送 | 待执行 | 用户已于 2026-09-17 明确授权发布 v1.3.0；完成后回填提交、标签和 Release 链接 |

## 开工基线

- Xcode 27 license：已接受。
- 直接 `swift test`：因 iCloud `.build/out` 扩展属性导致测试包签名失败，不作为代码失败。
- 仓库外 scratch path 基线：310 项通过，0 项失败，其中 Core 271 项、App 39 项。
- 首轮 1.3.0：Core 317 项、App 67 项，合计 384 项通过，0 项失败。
- 回归修订后：Core 325 项、App 99 项，合计 424 项通过，0 项失败。
- 第三轮微调后：Core 325 项、App 100 项，合计 425 项通过，0 项失败。
- 后续测试统一使用 `swift test --scratch-path <仓库外临时目录>`。
- 渲染证据统一写入 `$TMPDIR/Minget-1.3.0-Evidence/`；历史 `docs/versions/1.0.2/evidence/` 不再作为任何测试的输出位置。

## 真实验收记录模板

### ChatGPT 账号 A

- 验收时间：
- Profile ID：`chatgpt-a`
- 账号标识：仅记录“与预期账号一致/不一致”，文档不写真实邮箱
- 5 小时/周额度：一致 / 不一致
- 菜单栏切换：通过 / 未通过
- 点火请求：成功 / 失败
- 新窗口证据：已确认 / 未变化 / 无法确认

### ChatGPT 账号 B

- 验收时间：
- Profile ID：`chatgpt-b`
- 账号标识：仅记录“与预期账号一致/不一致”，文档不写真实邮箱
- 5 小时/周额度：一致 / 不一致
- 菜单栏切换：通过 / 未通过
- 点火请求：成功 / 失败
- 新窗口证据：已确认 / 未变化 / 无法确认

### DeepSeek

- 验收时间：
- 菜单栏币种：
- 与详情余额一致：是 / 否
- 失败或离线状态：正确 / 不正确

真实邮箱、余额、API Key、OAuth token、原始响应、Codex CLI session id 和点火 raw output 禁止写入本仓库。
