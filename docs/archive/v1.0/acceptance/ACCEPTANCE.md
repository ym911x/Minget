# UsageMonitor v1.0 最终验收

验收日期：2026-09-10  
版本：1.0.0  
结论：通过

## 结论依据

### 用户实际运行验证

- 用户确认该版本已跑通，并将其确定为第一个可用的正式版本。
- DeepSeek 在输入有效 API Key 后显示真实余额。
- 智谱 GLM 在应用内完成控制台登录后显示真实余额。
- 智谱 GLM 实测界面显示余额 `22.29921165 CNY`，并显示累计充值、累计赠送、累计消费和冻结金额。

### 自动化与构建验证

- `swift test`：274 项测试通过，0 项失败。
- `./scripts/build.sh`：Release 应用构建成功。
- `plutil -lint`：应用 `Info.plist` 校验通过。
- `codesign --verify --deep --strict`：应用签名结构校验通过。
- 应用版本：`CFBundleShortVersionString = 1.0.0`，`CFBundleVersion = 1`。

### 范围核对

| 能力 | 结果 |
| --- | --- |
| Codex 账号和额度展示 | 通过 |
| 菜单栏额度展示 | 通过 |
| 刘海安全区域和状态项位置检查 | 通过自动化与代码审核 |
| DeepSeek 余额显示 | 通过用户实际运行验证 |
| 智谱 GLM 控制台余额显示 | 通过用户实际运行验证 |
| Keychain 与敏感日志保护 | 通过自动化与代码审核 |
| 手动刷新和自动刷新 | 通过 |
| Release 应用构建 | 通过 |

## 已知限制

- 智谱控制台内部接口可能随官网改版而变化，后续需保留结构变化诊断和适配能力。
- 智谱登录会话的长期有效时间尚未完成连续多日测量。
- DeepSeek 密钥连接流程曾出现输入焦点和保存后窗口未及时关闭的问题，核心取数已可用，交互改善列入后续候选。
- 当前归档仅证明 Apple Silicon 本机构建可用，尚未完成 Developer ID 公证和跨设备分发验证。
