# 1.1.1 需求

日期：2026-09-13

1. 仅保留 OpenAI Codex 与 DeepSeek 数据支持，移除智谱 GLM 的运行时、界面和网络路径。
2. 设置页合并为单一“服务”模块。OpenAI Codex 固定显示，DeepSeek 提供详情显示开关与连接管理。
3. 底部退出文案为“退出明明有数”。
4. 关于弹层显示并可打开 `https://github.com/ym911x/Minget`，支持 ESC 和弹层外点击关闭。
5. 升级时清理本应用旧 GLM Keychain 项、缓存和偏好。清理失败不阻止启动，并在下次启动重试。

不包含推送、发布、标签或真实服务账号的自动验证。
