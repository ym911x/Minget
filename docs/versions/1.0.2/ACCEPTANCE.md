# 明明有数 · Minget v1.0.2 验收台账

四个阶段相互独立，任一项的结论不得用于替代另一项。当前状态：自动测试、候选构建、Codex 独立审核、钥匙串真实验收与菜单栏真实点击验收均已完成。

> 2026-09-11 追加：`KEYCHAIN_REVISION_PLAN.md`（钥匙串重复弹框修订）已实施 P0 与 P1，自动化测试增至 359 项。用户已确认首次授权后不再重复弹出，详见本文第 2.1 节。

## 1. 自动测试与本地构建

| 项目 | 状态 | 实际结果 |
| --- | --- | --- |
| `swift test` | 已完成 | 359 项通过，0 项失败，0 项跳过（含四项修订 345 项与钥匙串修订新增 14 项） |
| `./scripts/build.sh` | 已完成 | release 构建成功：staging 严格校验通过，运行候选包安装到 `~/Applications/Minget.app` 并再次严格校验，`dist/Minget.app` 为归档副本 |
| `codesign --verify --strict` 运行候选包 | 已完成 | `valid on disk`；两个嵌套可执行文件单独校验通过 |
| `codesign --verify --verbose=1 dist/Minget.app` | 已完成 | `valid on disk`（归档副本；严格校验受 iCloud file provider 属性影响，见 `evidence/logs/12-keychain-revision-build.txt`） |
| `CFBundleShortVersionString` | 已完成 | `1.0.2` |
| Bundle Identifier | 已完成 | `local.usagemonitor.UsageMonitor`，与 1.0.0／1.0.1 一致 |
| 签名身份 | 已完成 | `Minget Local Signing`（自签名证书，SHA-1 `5A48BB2B…`），DR 含证书根 `5a48bb2b…` |
| 二进制 | 已完成 | `Mach-O 64-bit executable arm64` |

原始输出见 `evidence/logs/01-test-and-repo.txt`、`02-build-and-bundle.txt`、`09-signed-verification.txt` 与 `12-keychain-revision-build.txt`。1.0.0 的 274 项测试记录未被改写。

## 2. 真实服务与真实界面

运行包：`dist/Minget.app`，版本 1.0.2，复制到 `/tmp/minget-run/` 后启动，以排除仓库路径与旧包干扰。原始日志见 `evidence/logs/03`、`04`、`05`。

| 编号 | 检查内容 | 状态 | 依据 |
| --- | --- | --- | --- |
| UI-01 | 正常完整菜单栏从 5H 起，无 M²，无空图标占位 | 部分验证 | 真实进程日志确认 `mode:full` 且文字实测宽度 100 pt（`5H 78% \| W 42%`），布局由夹具图 `01`／`02` 呈现；真实截图缺失 |
| UI-02 | 上排 5 段、下排 7 段，两端对齐完整文字 | 夹具验证 | 夹具图 `01`、`02`、`05`、`06`、`07`、`08`；含 22 pt 裁切带对照图 |
| UI-03 | 方向：2h 时左两段亮，2.5h 时第三段半亮，右端先空 | 夹具验证 | 夹具图 `03`（5 小时行 7 个时间点）与 `04`（周行 5 个时间点） |
| UI-04 | 实时依据为系统时间与服务 `resetsAt` | 已完成 | 真实进程日志记录 `fetch ok windows=5h:true/weekly:true`；夹具图使用固定锚点时间，公式见 `IMPLEMENTATION_REPORT.md` 第 2 节 |
| UI-05 | 到期后条变空并等待刷新，无本地假满格 | 夹具验证 | 夹具图 `03`、`04`、`05` 的「已到」行；单元测试 `ResetTimeProgressTests` 覆盖 |
| UI-06 | 前方唯一警告，尾部无警告，未知时间可辨识 | 夹具验证 | 夹具图 `01`、`02`、`05`、`06`；单元测试 `MenuBarContentTests` |
| UI-07 | 浅色／深色 | 夹具验证 | 夹具图 `01`／`02`、`05`／`06`、`07`／`08` 成对提供。高对比、缩放、外接屏未覆盖 |
| UI-08 | 9%、100%、未知占位、警告变化后不裁切不抖动 | 夹具验证 | 夹具图 `01`、`05`、`07`、`08`；宽度顺序由 `MenuBarLabelWiringTests` 覆盖 |
| UI-09 | compact 仍有两排；最小兜底显示 5H；恢复空间不振荡 | 夹具验证 | 夹具图 `01`／`02` 的 compact 与最小兜底行；防抖由 `MenuBarSpaceStateTests` 覆盖。修复前的真实日志 `04` 显示过最小兜底被误触发，修复后日志 `03` 稳定停在 full |
| UI-10 | 外部点击桌面或其他应用 | **用户实测通过** | 2026-09-12 用户确认详情立即收起且原点击有效；其他状态项与独立窗口由纯几何判定测试覆盖 |
| UI-11 | 内部交互、Esc、再点状态项 | **未验证** | 同上 |
| UI-12 | 连续 20 次开关无监听累积 | 部分验证 | 监听幂等与清理由 `MenuBarLabelWiringTests` 覆盖；真实 20 次点击未执行 |
| UI-13 | 睡眠唤醒后按实际时间重算 | **未验证** | 未执行真实睡眠周期；唤醒通知注册中心已修正，重算逻辑由 `ResetTimeProgressTests` 覆盖 |
| UI-14 | GLM 登录窗口与普通详情窗口不被误关 | **未验证（真实交互）** | 代码路径上独立窗口不参与弹层判定，由纯判定测试覆盖 |

截图缺失原因：本环境无屏幕录制权限，`screencapture` 无法执行；无辅助功能权限，System Events 合成点击返回「权限违例」。需求文件 §8.2 允许在环境无法覆盖时记录未验证项，本轮按此执行，未为截图而修改任何全局系统设置。

## 2.1 钥匙串修订（`KEYCHAIN_REVISION_PLAN.md`）验收

| 场景 | 状态 | 依据 |
| --- | --- | --- |
| 初始化与反复读取报告不直接调用凭证存储 | 已验证（自动测试） | 状态查询零钥匙串访问，`CredentialAccessTests` |
| 多处同时刷新：同一凭证读取合并 | 已验证（自动测试） | 并发两路读取合并为一次访问 |
| 拒绝/取消：停止自动重试，可恢复，不丢失凭证，不显示伪余额 | 已验证（自动测试） | 受阻状态记忆与写失败传播 |
| 代码变更后构建：同证书、CDHash 变化、DR 稳定、严格校验 | 已验证（实测） | 新代码 CDHash `817cc257…`，DR 证书根仍 `5a48bb2b…` |
| 日志：正常 open 可采集调用级证据，无敏感信息 | 已验证 | 用户开启开关后经真实启动采集成功（构建身份、指定要求、交互模式、结果类别）；关/开两种状态均记录（曾发现关闭动作不写日志的缺陷，已修复） |
| 完全退出再 open 无重复授权框，真实余额可读取 | **已验证（用户实测）** | 仅弹框一次，「始终允许」后不再弹；授权后缓存 `lastSuccessAt` 于 23:11:34 与 23:26:45 两次更新，证明真实读取发生 |
| 后台轮询、开关面板 20 次无新授权框 | **待真实界面** | 机制已就绪（拒绝即暂停 + 结果记忆），需用户抽查 |
| 换 Key、断开、重新登录的状态一致性 | 部分验证（自动测试） | 真实流程待用户 |
| 锁屏/唤醒不连弹 | **待真实界面** | 机制已就绪，需用户抽查 |

运行候选包：`~/Applications/Minget.app`（严格校验通过）。真实弹框类项目的执行方式：打开该包，按面板提示完成各条目的一次授权，然后完全退出并连续 `open` 三次核对是否不再弹框。

## 3. 用户最终体验验收

状态：**已完成**。钥匙串弹框已由用户实测通过；2026-09-12 用户确认菜单栏详情在点击桌面或其他应用后立即收起，且原点击有效。两排条的样式与方向按用户确认运行版本验收。

## 4. 发布

状态：**已完成**。用户给出的条件已经满足，代码与版本资料已于 2026-09-12 推送至 `origin/main`。Git 标签和 GitHub Release 不在本次上传动作内。

## 5. 结论

1.0.2 代码、自动测试、候选构建、独立审核和用户真实交互验收均已完成，可以提交并推送 GitHub。真实菜单栏截图仍未归档，不影响用户已完成的实际交互确认。
