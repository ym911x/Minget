# 1.4.1 实施任务

状态：代码、自动验证、Release 构建、签名包核验和真实界面验收已完成。OpenAI 与 DeepSeek Logo 已按图案可见边界修订，用户于 2026-09-21 确认效果并授权正式发布。

## 1. 详情页布局

- [x] 将详情页保持为 440 pt 单列，按 ChatGPT A、ChatGPT B、DeepSeek、Command Code 顺序排列。
- [x] 增大标题、额度和辅助文字，补齐组内 4 pt、周期 10 pt、头部／摘要 12 pt 留白。
- [x] 删除卡片内分隔线，保留卡片轮廓和背景层次。
- [x] 让 5 小时时间条固定 5 格、周时间条固定 7 格，本月时间条连续显示。
- [x] 短屏使用卡片区唯一垂直滚动区域；首选高度足够时不创建滚动容器。

## 2. 服务卡片

- [x] ChatGPT 改为两行头部：自定义名称；套餐与连接绿点；邮箱右对齐。
- [x] DeepSeek 改为两行头部，加入“服务器正常”及官方状态页外链。
- [x] Command Code 统计拆为 Token／成本行和请求／结果行；月度文案去掉“剩余”。
- [x] 保留缓存、失败、未知状态的可读文字和辅助功能值。

## 3. 显示名称偏好

- [x] 新增 `DisplayNamePreferences`，以 Profile／服务 ID 为键写入版本化 UserDefaults。
- [x] 设置页新增四个输入项、保存、恢复默认、空白归默认和 40 字符校验。
- [x] 详情页、来源选择、点火计划标题、点火按钮辅助标签读取同一个偏好对象。
- [x] 增加持久化、恢复默认、非法 ID 和超长输入测试。

## 4. 验证与资料

- [x] 更新布局、滚动、时间条、月度文案和统计分组测试。
- [x] 生成明暗模式示例渲染到临时证据目录，不把账号和余额写入仓库。
- [x] 运行完整 `swift test` 并记录通过、跳过和失败数量：517 项执行，516 通过、1 项环境跳过、0 失败。
- [x] 运行 `scripts/build.sh`，检查 Release arm64 包版本、严格签名、安装包和进程状态；安装包版本为 1.4.1，`codesign --verify --strict` 通过。
- [x] 用户通过真实 1.4.1 界面确认整体布局和自定义名称显示正常，并指出 OpenAI Logo 偏小。
- [x] 保留 OpenAI 官方原始素材，新增只裁剪 SVG `viewBox` 的 UI 副本，以约 28 pt 可见图形直接矢量渲染，移除会降低清晰度的视图后置缩放。
- [x] 按 DeepSeek 鲸鱼的 64 × 48 px 实际图案边界裁去 92 × 84 px 图片画布空白和白底，保留 2 px 抗锯齿余量；34 pt 槽内可见图案约为 32 × 24 pt，左缘与其他卡片对齐。
- [x] 默认 Command Code 名称下不再重复显示品牌名。重新运行完整测试、Release 构建与严格签名，安装后重启应用。
- [x] 用户确认 OpenAI 与 DeepSeek Logo 的清晰度、比例和对齐，并授权正式发布 1.4.1。
